'use client';
import{useState}from'react';import Link from'next/link';import{AppNav}from'./app-nav';import{AppIcon}from'./app-icon';
export function AppShell({children}:{children:React.ReactNode}){const[open,setOpen]=useState(false);return <div className="app-shell"><button className="mobile-menu" onClick={()=>setOpen(true)} aria-label="Открыть меню" aria-expanded={open}><AppIcon name="menu"/></button>{open&&<button className="drawer-scrim" aria-label="Закрыть меню" onClick={()=>setOpen(false)}/>}<aside className={`app-sidebar${open?' is-open':''}`}><div className="sidebar-head"><Link href="/app" className="app-brand" onClick={()=>setOpen(false)}><span><AppIcon name="chair"/></span><div><b>MebelDocs</b><small>мебельный документооборот</small></div></Link><button className="drawer-close" onClick={()=>setOpen(false)} aria-label="Закрыть меню"><AppIcon name="close"/></button></div><div className="tenant-chip">Рабочая область · Казахстан</div><AppNav onNavigate={()=>setOpen(false)}/><div className="sidebar-foot"><span>ML</span><div><b>MebelLegal</b><small>юридический модуль</small></div></div></aside><div className="app-frame"><header className="app-topbar"><div><small>РАБОЧАЯ ОБЛАСТЬ</small><b>Документы и сроки — вокруг заказа</b></div><Link href="/app/orders/new" className="primary-action"><AppIcon name="plus" size={17}/>Создать заказ</Link></header><main className="app-content"><div className="prototype-note"><strong>Рабочий прототип.</strong> Договор необязателен; AI/LLM и внешняя отправка отключены.</div>{children}</main></div></div>}
