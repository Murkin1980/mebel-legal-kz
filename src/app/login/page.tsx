'use client';
import{useState}from'react';import{useRouter}from'next/navigation';import{createClient}from'@/lib/supabase/client';
export default function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[message,setMessage]=useState<string|null>(null);const[loading,setLoading]=useState(false);const router=useRouter();const supabase=createClient();
async function signIn(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage(null);const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setMessage('Неверный email или пароль');setLoading(false);return}router.push('/app');router.refresh()}
async function signUp(){setLoading(true);setMessage(null);const{error}=await supabase.auth.signUp({email,password});setMessage(error?'Не удалось создать аккаунт. Проверьте данные.':'Проверьте email для подтверждения аккаунта.');setLoading(false)}
return <main className="login-page"><section className="login-visual" aria-label="О продукте"><div className="brand"><span className="brand-mark">MD</span><b>MebelDocs</b></div><div className="login-copy"><span>Документооборот мебельной компании</span><h1><span className="sr-only">MebelDocs — </span>Весь заказ.<br/>Все документы.<br/>Один процесс.</h1><p>Счета, акты, необязательный договор, сроки и напоминания связаны с заказом — от первого замера до закрытия работ.</p></div><div className="login-furniture">▱ Кухни · Шкафы · Мягкая мебель · Проекты на заказ</div></section><section className="login-panel"><div className="login-card"><h2>Войти в рабочую область</h2><p>Продолжите работу с заказами и документами вашей компании.</p><form className="login-form" onSubmit={signIn}><label htmlFor="email">Email<input id="email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label htmlFor="password">Пароль<input id="password" type="password" autoComplete="current-password" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/></label>{message&&<div className="login-error" role="status">{message}</div>}<div className="login-actions"><button className="login-primary" type="submit" disabled={loading}>{loading?'Вход…':'Войти'}</button><button className="login-secondary" type="button" onClick={signUp} disabled={loading}>Регистрация</button></div></form></div></section></main>}
